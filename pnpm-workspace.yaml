packages:
  - "bindings/node"
  - "packages/*"
  - "examples/fastify"
  - "apps/*"
  # examples/npm-smoke and examples/hono-npm are intentionally outside the workspace
  # so they install @monorch/ai from the npm registry instead of linking packages/ai.
